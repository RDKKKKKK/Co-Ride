<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.carpool.mapper.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="idUser">
        insert into user (accountNo, password, name, idOrganization, phone)
        VALUES (#{accountNo}, #{password}, #{name}, #{idOrganization}, #{phone})
    </insert>

</mapper>